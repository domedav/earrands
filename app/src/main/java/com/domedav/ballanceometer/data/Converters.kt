package com.domedav.ballanceometer.data

import androidx.room.TypeConverter

class Converters {
    // No converters needed: recurrence stored as String (Recurrence.name)
    // Placeholder for future type converters if required
}
